Needs doing:
  - add NLPD

UI
  - Export images
  - Tool bar
     - image display size
     - calibration
    - Progress bar force update
    - kill avg plot graph calc when next images press to make more responsive UI (add these as threads we can kill)


minor:
  - fix metric images being stretched (muliptle rows)
  - Option to change image display size
  - Option to change screen calibration

  - get metrics and metric images from all data stores
  - stay on current tab for plots when resetting UI
